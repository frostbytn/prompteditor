{
  "name": "promptbin",
  "version": "1.0.0",
  "scripts": {
    "build": "tailwindcss -c tailwind.config.js -i src/input.css -o dist/output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.0"
  }
}
